{
  "buildCommand": "EXPO_PUBLIC_BUILD_PLATFORM=web npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "cd .. && npm install -g pnpm@10.32.1 && pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
