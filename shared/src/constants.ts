export const EMOJIS = ['🍎', '🍊', '🍋', '🍇', '🍓', '🐱', '🐶', '🐰', '🌟', '🦋', '🌸', '🍦'];

export const DEFAULT_TOTAL_ROUNDS = 10;

/** Wall-clock timeout per round in ms. */
export const ROUND_TIMEOUT_MS = 10_000;

/** Answers arriving within this delta (ms) are scored as a tie. */
export const TIE_THRESHOLD_MS = 100;

export const POINTS_PER_CORRECT = 10;
