{
  "buildCommand": "pnpm --filter ./app build:web",
  "outputDirectory": "app/dist",
  "framework": null,
  "installCommand": "npm install -g pnpm@10.32.1 && pnpm install --frozen-lockfile",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
